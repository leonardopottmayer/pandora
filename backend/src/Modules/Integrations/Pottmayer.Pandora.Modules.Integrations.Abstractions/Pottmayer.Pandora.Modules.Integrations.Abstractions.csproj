<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Ports hand back Result<T>, so consumers get a typed failure instead of an exception. -->
    <PackageReference Include="Pottmayer.Tars.Core.Primitives" Version="0.0.8" />
  </ItemGroup>

</Project>
